# STAGED ONLY - this file is an example and is not active.
# The site is staged and requires certificates before activation.
APP_NAME=Ringlite
PUBLIC_BASE_URL=https://ringlite.io
PUBLIC_WEB_URL=https://ringlite.io
CORS_ORIGINS=https://ringlite.io
WEBAUTHN_RP_ID=ringlite.io
LIVEKIT_PUBLIC_URL=wss://ringlite.io/livekit
# Didit KYC overlay: selects the business default identity provider and
# the Didit workflow to use. Existing API key / webhook secret settings
# (e.g. DIDIT_API_KEY, DIDIT_WEBHOOK_SECRET) are managed outside this
# overlay and are intentionally left untouched.
KYC_IDENTITY_PROVIDER=didit
DIDIT_WORKFLOW_ID=fc10938c-a05d-41da-926f-36307ef5d9d5
